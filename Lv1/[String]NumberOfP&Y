def solution(s):
    p_num = 0
    y_num = 0
    for i in s.lower():
        if i == 'p':
            p_num = p_num +1
        elif i == 'y':
            y_num = y_num +1
    answer = True if p_num == y_num else False 
    return answer
    
"""
#count함수를 잊어버렸다..!
def numPY(s):
    return s.lower().count('p') == s.lower().count('y')
"""
